# Admin API

The admin panel's internal API at `/api/admin/` handles all management operations (kick, ban, broadcast, marketplace, etc.). These endpoints require admin authentication via session cookie and are documented in the [Admin Panel](../server/admin-panel.md) page.
